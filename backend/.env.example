# ⚠️ IMPORTANTE: Crea tu propio archivo .env
# Copia este archivo como .env y completa con tus credenciales reales

# MongoDB Configuration
# Obtén tu connection string en: https://cloud.mongodb.com
# Formato: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://tu-usuario:tu-password@cluster0.xxxxx.mongodb.net/lorefit?retryWrites=true&w=majority

# JWT Configuration
# Genera un secret seguro (puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=tu-secret-super-seguro-aqui-cambiar-esto
JWT_EXPIRE=30d

# MercadoPago Configuration
# Obtén tu Access Token en: https://www.mercadopago.com.ar/developers/panel/app
MERCADOPAGO_ACCESS_TOKEN=TEST-1234567890-abcdef-ghijklmnop-qrstuvwxyz
MERCADOPAGO_SANDBOX=true

# Server Configuration  
PORT=3000
BASE_URL=http://localhost:3000

# Notas:
# - Para pruebas, usa MERCADOPAGO_SANDBOX=true y el Access Token de TEST
# - Para producción, cambia a MERCADOPAGO_SANDBOX=false y usa el Access Token de PROD
# - El BASE_URL debe coincidir con tu dominio en producción
# - Cambia JWT_SECRET por un valor aleatorio y seguro
# - No compartas tu archivo .env con nadie
